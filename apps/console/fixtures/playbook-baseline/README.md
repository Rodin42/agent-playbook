# playbook-baseline — seed from the 2026-09-05 factory test

`test-prompt.txt` is the exact prompt composed from the product-strategist role doc +
the feature-plan entry format + one instruction; `test-entry.md` is what
`claude -p --model haiku` returned (13/13 required fields). The demo fixture's F-001
entry is derived from it, so at least one fixture entry is model-authored, not
hand-written.
