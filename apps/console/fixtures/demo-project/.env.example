# FIXTURE ONLY — every value below is a dummy. Copy to .env so the overview shows "set".
CLAUDE_CODE_OAUTH_TOKEN=fixture-dummy-not-a-token
E2B_API_KEY=fixture-dummy-not-a-key
GITHUB_TOKEN=fixture-dummy-not-a-token
