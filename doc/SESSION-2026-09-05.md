# Session snapshot — 2026-09-05 (agent factory runtime test)

Written so the next session can resume without re-explaining. Companion file:
`doc/frontend-and-tooling-research.md` (the Langflow-alternatives research).

## What was done today

### Factory runtime test (steps 1–6 of the test plan) — PASSED
| Check | Result |
| --- | --- |
| `claude --version` | 2.1.261 |
| `claude -p --model haiku "Reply with exactly: FACTORY-OK"` | `FACTORY-OK` |
| `pi --version` | 0.85.1 (`/opt/homebrew/bin/pi`), installed `--ignore-scripts`, **no auth, no extensions** |
| `e2b --version` | 2.18.0 (`/opt/homebrew/bin/e2b`) |
| Playbook role run headless (product-strategist) | 13/13 feature-plan fields present |

### Environment change
`npm -g` failed with EACCES: node was the official `.pkg` build in `/usr/local`
(root:wheel). Fixed by installing node via Homebrew (26.8.1 at `/opt/homebrew/bin`),
which shadows the old `.pkg` node 24.18.0. Old node left in place, harmless.

### Token-in-container test — PASSED (the key Phase B unblock)
```
control (no token)  -> "Not logged in · Please run /login"
with token          -> SANDBOX-OK
```
Command used (token passed by env, never in argv or shell history):
```
read -rs CLAUDE_CODE_OAUTH_TOKEN
export CLAUDE_CODE_OAUTH_TOKEN
docker run --rm -e CLAUDE_CODE_OAUTH_TOKEN node:22 \
  sh -c 'npm i -g @anthropic-ai/claude-code >/dev/null 2>&1 && \
         claude -p --model haiku "Reply with exactly: SANDBOX-OK" </dev/null'
```
=> The Max subscription authenticates Claude Code headless in a container.
   The claude-code harness path is verified end to end.

**Finding to build into the orchestrator:** `claude -p` exits **0 even on auth
failure**. Never trust exit status; verify the expected artifact was written.

### Docs fixed
`runtime-plan.md` — 8 lines corrected. It described **pi** as the template default in
seven places, contradicting `runtime/factory.defaults.yaml`, which ships
`harness.provider: claude-code`. Also made the §1 run-shape step 4 harness-agnostic.
Original backup: scratchpad `runtime-plan.bak` (session-temp, may be gone).

### Repo move
- `wiersh/agent-playbook` (work account, Azure) — **untouched**, remote removed locally.
- New: `git@github.com:Rodin42/agent-playbook.git`, private, single commit `748a7f2`
  authored `Rodin Lie <rodin.lie@gmail.com>`. Repo was deleted and recreated empty so
  no old objects ever existed there. Local reflog scrubbed (`gc --prune=now`).
- Local identity pinned: `git config --local user.email rodin.lie@gmail.com`.
- `.gitignore` gained `factory-test/` and `.DS_Store`.
- `factory-test/` = local-only fixture, 2 commits, gitignored, not pushed.

## Open finding: status vocabularies are inconsistent
`runtime-plan.md` §3 says the dashboard renders a feature's stage "from artifact status
fields", but the seven Phase-2 templates declare four different vocabularies and three
declare none:
```
brainstorming.md        draft | reviewed | final
implementation-plan.md  draft | ready | superseded
implementation.md       in-progress | in-review | qa | merged
masterplan.md           draft | ready
architecture-analysis   draft   (no vocabulary declared)
edge-case-analysis      draft   (no vocabulary declared)
online-research         draft   (no vocabulary declared)
```
`CONTEXT.md` parks "feature `status` state machine" as a later improvement. The
orchestrator needs it too (to know a step succeeded), not just the dashboard.

## Manual to-dos
- [x] `CLAUDE_CODE_OAUTH_TOKEN` in password manager
- [x] `E2B_API_KEY` in password manager
- [ ] Create `.env` when cloning into the real project (`cp .env.example .env`, paste
      with an editor — not `echo >>`, which writes secrets to `~/.zsh_history`).
      Do **not** set `ANTHROPIC_API_KEY` alongside `CLAUDE_CODE_OAUTH_TOKEN`.
- [ ] Build e2b `factory-base` template
- [ ] Fill `substrate.md` and `rodin-twin.md` (content, blocks Phase 2 + the Ready gate)

## Next steps discussed
1. Unify the status vocabulary across the seven templates (blocks orchestrator + UI).
2. Write a one-page state contract: stages, gate verdicts, where truth lives.
3. `factory-base` e2b template — node + git + claude-code is enough for **Phase 1**
   roles (markdown only). The project toolchain is only needed for Phase 2, so the
   template is NOT blocked on `substrate.md`.
4. Orchestrator v0: sandbox -> clone -> compose prompt -> run harness -> collect
   artifact -> commit -> destroy.
5. Frontend (Phase D) — see the research file.
