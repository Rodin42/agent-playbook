# <project> — the full local check is `make check` (lint + typecheck + test + build).
# These targets are the command contract recorded in phase-1-product-development/substrate.md §6
# and called by .github/workflows/factory-ci.yml. Fill them in start_prompt Stage 3; change all
# three together afterwards. Every target must run from the repo root and exit non-zero on failure.
.PHONY: install lint typecheck test build check

install:            ## install every toolchain's dependencies
	@echo "TODO: fill from substrate.md §6" && exit 1

lint:               ## static style checks
	@echo "TODO: fill from substrate.md §6" && exit 1

typecheck:          ## static type checks
	@echo "TODO: fill from substrate.md §6" && exit 1

test:               ## tests
	@echo "TODO: fill from substrate.md §6" && exit 1

build:              ## produce runnable artifacts (echo when a toolchain has no build step)
	@echo "TODO: fill from substrate.md §6" && exit 1

check: lint typecheck test build   ## the full local check
